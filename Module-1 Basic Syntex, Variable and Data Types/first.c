#include <stdio.h>

int main() {
    // // Input
    // int a;
    // float b;
    // char c;

    // scanf("%d %f %c", &a, &b, &c);
    // // Output
    // printf("%d, %.2f, %c", a, b, c);

    // long long int a;
    // scanf("%lld", &a);
    // printf("%lld\n", a);

    // double b;
    // scanf("%lf", &b);
    // printf("%lf\n", b);

    long long int a;
    double b;
    scanf("%lld", "%lf", &a, &b);
    printf("%lld, %lf\n", a, b);


    return 0;
}