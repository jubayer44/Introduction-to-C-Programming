#include <stdio.h>
#include <math.h>
int main()
{
    int cl = ceil(10.22);
    int flr = floor(10.80);
    int rund= round(10.40);
    int power = pow(9, 2);
    double squear = sqrt(50); 
    int absolute_val = abs(-10); // ignore negative value
    return 0;
}